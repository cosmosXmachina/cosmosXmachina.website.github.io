export function disclosure(language) {
  return language === "en"
    ? "Synthetic demonstration. Orion Works is fictional and all data is synthetic."
    : "Dimostrazione sintetica. Orion Works e fittizia e tutti i dati sono sintetici.";
}

export function modeLabel(language) {
  return language === "en" ? "Synthetic demonstration" : "Dimostrazione sintetica";
}
